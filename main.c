#include "main.h"
#include <limits.h>
#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0
 */
int main(void)
{
    int len;
    int len2 = 0;

    len = _printf("Let's try to printf a simple sentence.\n");
    printf("Length:[%d, %i]\n", len2, len2);

    return (0);
}